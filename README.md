# AWS-Serverless-Email-Automation-Platform
Built a serverless contact form using AWS Lambda, API Gateway, and Amazon SES. The application validates user input, processes form submissions through a REST API, and automatically sends email notifications while leveraging IAM roles, CORS, and CloudWatch monitoring.
